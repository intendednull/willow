[package]
name = "willow-network"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "P2P networking layer for Willow using iroh"

[features]
test-utils = []

[dependencies]
willow-identity = { path = "../identity" }
iroh-base = { workspace = true, features = ["key"] }

async-trait = { workspace = true }
bytes = { workspace = true }
blake3 = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
serde = { workspace = true }
futures-lite = "2"

# Native-only: iroh networking stack (brings in tokio/mio).
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
iroh = { workspace = true }
iroh-gossip = { workspace = true }
iroh-blobs = { workspace = true }
tokio = { workspace = true }

# WASM: only need the type definitions from gossip/blobs.
[target.'cfg(target_arch = "wasm32")'.dependencies]
iroh-gossip = { workspace = true, default-features = false }
iroh-blobs = { workspace = true, default-features = false }

[dev-dependencies]
tokio = { workspace = true, features = ["full", "sync"] }
tracing-subscriber = { workspace = true }
